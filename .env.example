# Copy to .env (docker compose picks it up automatically). Defaults below work
# out of the box for a local demo; change the secrets for any shared deployment.
MYSQL_ROOT_PASSWORD=rootpw
MYSQL_DATABASE=surveillance
MYSQL_USER=surveillance
MYSQL_PASSWORD=surveillance

DJANGO_DEBUG=True
DJANGO_SECRET_KEY=dev-insecure-key-change-in-production
